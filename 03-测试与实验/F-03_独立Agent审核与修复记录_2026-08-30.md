# F-03 独立Agent审核与修复记录

## 裁定

- 旧候选：`7950a1fbd1035f8d80c94647d6849dab535aa07c`
- 旧候选独立审核：`FAIL`
- 新候选：`a61eba6a62631780caaf60d8e2e431326d3082ba`
- 当前结论：`AUTOMATION_PASS / INDEPENDENT_REAUDIT_PENDING`
- 治理状态：`IN_REVIEW`；不得据此标记`DONE`

## 旧候选发现与关闭证据

| 发现 | 等级 | 新候选处理 | 证据 |
|---|---|---|---|
| 环境锁直接使用原始字节哈希，干净检出因CRLF变化失败 | P1 | 与独立任务包统一为`sha256_lf_no_trailing_ws_text_v1`，真实内容变化继续失败关闭 | `Tools/F03/tests/F03Environment.Tests.ps1`、`environment_report.json` |
| 正式门日志与资产报告并非同次运行 | P1 | 正式门通过受控环境变量保留同次报告，脚本强制比对日志和JSON计数 | `formal_negative_report.json`、`asset_blocking_report.json` |
| 运行证据未排除未提交输入 | P2 | 所有入口要求运行前Git干净，并记录提交、实现树哈希和运行后范围 | `run_identity_report.json` |
| 双构建仅保留清单，无法逐文件复核 | P2 | 两份339文件本地产物保留至第二人复核，脚本已逐文件重算哈希 | `build_artifact_receipt.json` |
| F-01缺失必填字段拒绝没有独立日志 | P2 | 保留完整专项pytest输出并记录日志哈希 | `f01-contract-negative.log`、`formal_negative_report.json` |

## 修复过程中新增并关闭的发现

1. 全新工作树首次恢复Git包时发生一次TLS/RPC中断；远端恢复后锁定提交成功导入，未修改包版本或锁文件。
2. Unity首次导入写回5项序列化迁移，并将4项受控文本改为LF。迁移已纳入候选，Unity YAML/JSON固定LF。
3. 新候选完整运行后只有`03-测试与实验/evidence/F-03/`发生变化，Unity源文件和项目设置无运行后漂移。

## 当前自动验证事实

- F-03工具回归：Pester `6/6`。
- F-01合同专项：pytest `49 passed`；完整运行另保留必填字段拒绝日志。
- Unity Edit Mode：`3/3`；Play Mode：`1/1`。
- Windows `DEV-REPLAY`：两次构建均为339个声明文件，文件集合一致，产物逐文件哈希通过。
- Player：启动、截图和端口隔离通过。
- 未授权Development构建与正式门均按预期拒绝；正式门同次资产报告为189项、21个阻断。

## 剩余硬门

1. 新候选须由独立Agent重新只读审核并给出`PASS/FAIL`。
2. 独立Agent通过后，傅钧烨须基于同一候选执行第二人复核并签署。
3. 只有第二人`PASS`后，F-03才可迁移为`DONE`。

本记录只证明Unity环境、测试和Windows `DEV-REPLAY`基线；不证明正式构建、实时通信、四场景完整旅程或`LIVE_E2E`。
