# IN_REVIEW Agent独立审核报告（2026-08-16）

> 审核性质：三个独立Agent对固定实现提交执行只读复核
>
> 总结论：`MODEL_REVIEW_FINDINGS_CLOSED / HUMAN_SECOND_REVIEW_PENDING`
>
> 状态影响：G-02、P-01、P-02继续保持`IN_REVIEW`；本报告不替代真实团队第二人签署

## 1. 审核对象与边界

| 任务 | 固定实现提交 | Agent | 结论 |
|---|---|---|---|
| G-02 | `befbdb698ef5cbeb6061315a4c912abdde4e6b08` | `01a0088c-6f58-7942-9a2d-ad0bd1e616e9` | `REVIEW_FINDINGS_OPEN` |
| P-01 | `8063afc7053a79a0d27fe8fc94159dbc5fc9a9e0` | `01a0088c-750a-7e33-9323-612eec6f152e` | `REVIEW_FINDINGS_OPEN` |
| P-02 | `3d740e99118d6a76993156f26bdf673144429025` | `01a0088c-7b18-79c0-b545-3cb4187f2e80` | `REVIEW_FINDINGS_OPEN` |

本轮共发现P0 0项、P1 4项、P2 4项、P3 2项。先前技术验收记录中的模型复核通过结论属于当时证据；本轮新鲜反例优先，问题关闭前不得据此转为`DONE`。

## 2. 开放发现

### G-02

| 编号 | 严重度 | 发现 | 影响与修复要求 |
|---|---|---|---|
| G02-A1 | P1 | `credentials.py:118`、`g02.py:113`允许对固定HMAC凭据重复执行`provision`并覆盖已有密钥 | 历史去重令牌可能无法重建。已有合法密钥时必须拒绝覆盖；轮换必须使用独立、显式、可审计流程，并加入重复初始化测试。 |
| G02-A2 | P2 | `privacy.py:32`只匹配完整字符串，漏过普通文本中嵌入的手机号或邮箱 | Manifest可能夹带联系方式。改为搜索型检测，并验证错误只返回原因码和JSON路径。 |
| G02-A3 | P3 | `G-02_第二人审核报告_待签署.md:38`的复核命令直接覆盖仓库内证据文件 | 只读复核边界不清晰。复核输出应写入系统临时目录，证据更新单独提交。 |

### P-01

| 编号 | 严重度 | 发现 | 影响与修复要求 |
|---|---|---|---|
| P01-A1 | P1 | `core.py:411,472`与`transport.py:445`在第四模块先转`COMPLETED`，随后`end`交付失败无法转为`ABORTED` | Unity未确认结束时可能被记录为完整完成。结束交付成功前保留非完成状态，或允许未确认完成转为中止；覆盖超时、拒绝和断连。 |
| P01-A2 | P2 | `core.py:514`与`transport.py:122,228`允许尚未发送的后续控制被提前ACK | 控制审计与真实发送顺序不一致。引入`CREATED -> SENT -> ACKED`生命周期，只接受当前已发送事件的ACK。 |
| P01-A3 | P2 | `core.py:545`未核对渲染回执的模块和分段是否与原控制一致 | 错配回执仍会被接受。必须比对原控制负载，并覆盖模块错配、分段错配和提前回执。 |

### P-02

| 编号 | 严重度 | 发现 | 影响与修复要求 |
|---|---|---|---|
| P02-A1 | P1 | `archive.py:158,626`未保护未封存`archive.json`中的全部关键元数据 | 修改`protocol_config_hash`后，`verify(mode="recover")`仍返回可恢复。应对完整envelope建立不可变哈希锚点，并在恢复前验证。 |
| P02-A2 | P1 | `archive.py:405,414,417`在锁外判断可恢复，存在恢复与正常封存的TOCTOU竞争 | 恢复动作可能在已封存归档中创建新段并导致完整性失败。取得写锁后必须重新打开、验证并检查封存状态，再创建新段。 |
| P02-A3 | P2 | `generate_stress_report.py:106,115`在主要对象释放后采集最终内存，`test_golden_archive.py:22`未冻结趋势阈值 | “无持续内存增长”证据无效。应在对象存活且归档关闭前采样，计算暖机后趋势并设定可审计阈值。 |
| P02-A4 | P3 | 当前P-02独立任务包`TASK.md:42`仍保留未回填复选框 | 任务包与技术验收记录缺少直接追踪。代码问题关闭后重新生成并回填固定提交、证据和剩余边界。 |

## 3. 复核验证

| 范围 | 结果 |
|---|---|
| G-02专项 | `107 passed`；179项资产、3个阻断组、`release_allowed=False`；正式环境6项缺失、`formal_ready=False` |
| P-01专项 | `82 passed`；golden为19个控制、19个ACK、12个回执、54个会话事件、800秒 |
| P-02专项 | `42 passed`；跨模块`288 passed`；800秒计数与基础完整性通过 |
| 全仓Python回归 | `331 passed` |
| 其他 | 协议权威、任务注册表、独立任务包及`git diff --check`通过；三个Agent结束时工作区状态与开始时一致 |

现有自动测试通过，但本轮反例证明测试集尚未覆盖上述边界。因此自动通过数量不能覆盖开放发现。

## 4. 修复与复审顺序

1. 先关闭G02-A1、P01-A1、P02-A1和P02-A2，并为每个反例加入失败测试。
2. 再关闭四个P2，补齐隐私搜索、控制交付生命周期、回执关联和内存趋势证据。
3. 最后修复两个P3治理问题，重生成受影响任务包和复核命令。
4. 使用新的固定修复提交重新启动三个独立Agent复审；只有返回`PASS_FOR_MODEL_INDEPENDENT_REVIEW`后，才进入真实团队第二人签署。

## 5. 保持开放的外部边界

- G-02专机环境、保留期限机构批准和3组Unity资产阻断未关闭。
- P-01不代表Unity、TouchDesigner、真实设备或`LIVE_E2E`已经通过。
- P-02不代表正式机器、真实数据采集链、Unity、TouchDesigner、X-01或完整在线链路已经通过。

## 6. P1 -> P2 -> P3快速修复与复审

旧发现按优先级依次修复，并在每次出现新反例后新增回归测试。统一代码复审提交为`03d7426219121e49554e405db9dd521b8ab1d819`。

| 范围 | 最终独立Agent | 结论 | 专项证据 |
|---|---|---|---|
| P-01 | `01a008fa-b83f-7982-ab82-b8fde66b8c93` | `PASS_FOR_MODEL_INDEPENDENT_REVIEW` | `98 passed`；并发交付、重复ACK、迟到end ACK、终态冻结和失败批次通过 |
| P-02 | `01a00910-6889-78e0-94d4-246e2409f328` | `PASS_FOR_MODEL_INDEPENDENT_REVIEW` | `68 passed`；恢复资源、路径、封存字段、重放配对和创建失败清理通过 |
| G-02 | `01a00914-d4c5-7fc1-8966-a400aaccba66` | `PASS_FOR_MODEL_INDEPENDENT_REVIEW` | `138 passed`；51项注册表、6个包/182个快照严格校验通过，G-02 63个快照一致 |

最终全仓Python回归为`404 passed`；任务注册表、协议权威和独立任务包校验通过，6个分发/复审包共182个输入快照无漂移。G-02资产门仍按预期失败关闭：179项机器清单，194个ignored发布文件和3个许可替换项。上述模型复审不替代真实团队第二人签署，三项任务继续保持`IN_REVIEW`。

P-01、P-02与G-02最终均无未关闭P0-P2模型复审发现。该结论只解除模型独立复审阻断，不解除真实团队第二人签署、正式专机、资产许可或完整在线链路门。
