---
status: accepted
date: 2026-08-23
---

# 累计状态在模块内生效并于模块间重置

每个天气模块拥有独立的累计状态：`demo`保持中性基线，`closed_loop`跟随Python累计值缓慢变化，结束时锁定实际结果并在`lock_transition`中保持，随后进入中性转场；下一模块从自身中性基线开始。锁定结果不强制达到100%，上一模块最终值只进入记录而不成为下一模块画面的初值，以保持四个场景顺序无关并符合累计层的模块级定义。
