# V-04 H3 可拆层背景重建验证记录 v1.0

> 日期：`2026-09-01 +08:00`
>
> 范围：四天气固定镜头背景的构图候选与后续切层边界，不包含透明层导出或Unity运行。

## 验证结果

| 检查项 | 结果 | 证据 |
|---|---|---|
| 四天气候选完整性 | `PASS` | 候选清单覆盖`storm`、`heat`、`snow`、`fade`各一项。 |
| 方向锁定 | `PASS` | 对应`B / C / C / C`选择记录。 |
| 固定镜头约束 | `PASS` | 清单要求`camera=FIXED`、`parallax_scroll=false`。 |
| 静态层和语义根边界 | `PASS` | 合同定义七个静态空间层与四个独立语义视觉根。 |
| `fade`背景选择 | `TEAM_DIRECTOR_SELECTED` | 团队总监选择当前`v5`；保留宽河谷、开放水道、左下浅水区域和固定镜头功能结构。 |
| 当前PNG尺寸和哈希 | `PASS` | `validate_v04_h3_layerable_background_rebuild.py`核对每张当前候选。 |

## 结论

`fade v5`已获得团队总监选择并进入当前清单。四张PNG均为`TEMP_REFERENCE_ONLY`完整构图参考，尚未生成透明分层文件，不能表述为Unity场景、正式构建、资产许可放行或真实输入链已完成。

## 下一硬门

`TEAM_DIRECTOR_REVIEW_THEN_LAYER_EXPORT`：确认四张候选后，按合同导出共享画布的透明静态层，并对切层叠合、来源和许可台账执行检查。
