# V-04 H3 R2大景深背景预览验证记录 v1.0

> 日期：`2026-08-31 +08:00`
>
> 结果：`PASS_FOR_HUMAN_SELECTION`

## 验证结果

| 检查 | 结果 |
|---|---|
| R2候选覆盖 | `PASS`：A/B/C三组各有`storm/heat/snow/fade`一张，共12张独立预览 |
| 镜头合同 | `PASS`：四天气均为`FIXED`，相机与背景位移为零，关闭世界视差卷动 |
| 本地制品完整性 | `PASS`：12张PNG均位于`.artifacts-local/V-04/H3/fixed-background-previews/r2/`，尺寸和SHA-256均与清单一致 |
| 机制可承载性筛查 | `PASS_FOR_SELECTION`：全部候选有近中远景；A-storm和A-snow已标为不推荐，避免固定背景预先占据雨幕语义或遮挡近场区 |
| R2预览校验器 | `PASS`：`validate_v04_h3_r2_previews.py` |
| V-04工具链 | `PASS`：冻结Python/Pillow/NumPy、FFmpeg与工具许可记录一致 |
| 历史H3合并机器门 | `PASS_HISTORICAL_EVIDENCE`：62秒候选与既有源门绑定仍完整；不表示固定背景重建后的H3已完成 |
| 协议权威 | `PASS`：v1.1与当前执行文件一致 |
| 任务注册表 | `PASS`：57项；`READY=F-03,F-05` |
| 独立任务包 | `PASS`：5包、132份快照 |
| 根回归 | `PASS`：冻结Python 3.14.4，`412 passed in 124.25s` |
| 静态检查 | `PASS`：新增内容受限措辞0命中、JSON解析、`git diff --check`通过 |

## 证据边界

结果只说明R2大景深构图、候选清单和项目静态基线可以进入团队总监选择。图像仍为`TEMP_REFERENCE_ONLY`，不表示分层背景、Unity运行、Windows制品、资产许可放行或真实输入链已经形成。

## 下一硬门

`H3_FIXED_BACKGROUND_PREVIEW_SELECTION`。团队总监逐天气选择A/B/C方向后，才能重建四张可拆层背景与新的固定镜头机制样片。
