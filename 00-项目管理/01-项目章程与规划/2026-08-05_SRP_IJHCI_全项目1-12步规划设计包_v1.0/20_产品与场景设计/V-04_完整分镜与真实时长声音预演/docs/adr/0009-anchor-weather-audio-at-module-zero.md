---
status: accepted
---

# 将天气声音锚定在模块零点

天气声在`lock_transition`归一化进度约`0.30`处以默认3秒等功率淡化到长廊声，长廊声持续覆盖中性山雾和下一天气基线显现。每个模块到达`demo`相对时间零点后才从自身天气母带零点启动，并以默认3秒从长廊声淡入；第一模块遵循相同入口，第四模块退出后保持长廊声。正式配置仍保留`2–4秒`范围，声音切换只由会话段事件触发。
