---
status: accepted
---

# 要求双提交候选复核与真实第二人签收

V-04在H3通过后以提交A冻结设计源、轨迹、工具锁、脚本和候选清单并作为GitHub预发布标签目标；附件上传和远端下载复核后，以提交B写回Release地址、附件ID、大小和哈希。任务迁移到`IN_REVIEW`后，独立Agent同时审查两个提交、候选清单和媒体附件，并按`P1→P2→P3`顺序修复；任何媒体变化创建新候选身份。只有`P0–P3`全部关闭、独立复核最终PASS后，真实团队第二人才可签署固定候选并触发`DONE`迁移。H3或自动验证均不单独构成任务完成。
