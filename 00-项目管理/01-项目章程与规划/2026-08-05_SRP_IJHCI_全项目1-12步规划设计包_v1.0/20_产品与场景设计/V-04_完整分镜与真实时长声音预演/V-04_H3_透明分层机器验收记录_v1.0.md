# V-04 H3 透明分层机器验收记录 v1.0

## 结果

| 字段 | 值 |
|---|---|
| 任务 | `V-04` |
| 硬门 | `H3_LAYERABLE_BACKGROUND_REBUILD` |
| 候选 | `layer-export-candidate-v2` |
| 机器结果 | `PASS` |
| 检查 | `18/18 PASS` |
| 透明PNG | `28` |
| 当前状态 | `READY_FOR_TEAM_DIRECTOR_REVIEW` |

## 验证范围

`validate_v04_h3_layer_exports.py`已经确认：

- 四天气各7层，28个`scene + layer_role`组合唯一且完整；
- 每张导出均为`3840 x 2160 RGBA PNG`；
- 非天空层同时包含透明和不透明像素，透明像素RGB为零；
- 所有权掩码互斥并覆盖全画布；
- 按冻结层序重组后与四张4K确认底片逐字节一致；
- 资产清单、包清单和文件SHA-256一致；
- 4K工作制品位于Git忽略目录且未被跟踪；
- 许可状态保持`PENDING_G02_CLEARANCE`，正式使用保持关闭。
- 候选身份与目录一致，包清单绑定当前生成器和配方哈希；
- 冻结源字节、派生配方和4K规范化主图与权威输入一致。

完整结构化结果见`review/H3/layer-export-candidate-v2/validation_report.json`，完整本地包清单的Git副本见同目录`package_manifest.json`。12张轻量评审图及28张4K导出引用由同目录`review_manifest.json`逐项绑定。

## 边界

机器门不判断单层空间语义是否自然，也不替代团队总监24项人工评审。本记录不表示Unity导入、Unity运行、正式构建、资产许可放行、真实输入链或V-04整体完成。
