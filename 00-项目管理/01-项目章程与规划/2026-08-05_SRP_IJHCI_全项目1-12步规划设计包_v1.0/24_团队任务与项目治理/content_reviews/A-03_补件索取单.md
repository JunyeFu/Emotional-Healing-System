# A-03 补件索取单

- 任务：A-03【统计实现】Gate2联合测量估计目标与 Monte Carlo 样本模拟
- 当前本地复核结论：`PARTIAL_PASS_WITH_BLOCKER`
- 当前可确认范围：`A-03-SPEC` 已在 `origin/codex/a-03-spec` 提交 `c3c4a89` 收口；证据类别为 `SYNTHETIC_ONLY`。
- 索取目的：补齐 A-03-REAL / A-03-CAL 的可审交付，或留下明确的依赖阻塞记录；不把合成结果写成真实研究结果。

## 向 A-03 作者索取

请在 `codex/a-03-spec` 或新的 A-03 工作分支提交以下材料，并回填 commit、运行命令与证据路径：

1. `A-03-REAL`：真实数据计分适配器（或可运行的真实数据入口）、输入版本与字段映射、盲态参数输入；不得提交可识别参与者数据。
2. `A-03-REAL`：真实数据接入的质量/缺失/技术未呈现处理记录，以及与 `A-03-SPEC` 的差异说明。
3. `A-03-CAL`：盲态校准回执、冻结分析配置、校准前后不可见参数边界与复现命令。
4. 若依赖尚未满足：在回复中明确列出阻塞项、责任人、所需上游提交和预计可交日期，不得以 `A-03-SPEC` 的合成报告代替。

## 向上游依赖门索取

- **A-01 依赖门（傅钧烨本人项目责任链）**：真实数据入口/字段字典/脱敏交接说明，以及允许 A-03-REAL 开始的依赖确认。
- **E-03 依赖门（傅钧烨本人项目责任链）**：Level C 材料或其冻结状态、盲态校准所需边界，以及允许 A-03-CAL 开始的依赖确认。
- **U12-04 责任人**：如 A-03-REAL 消费 PANAS/SAP 约定，请提供已合并的输入契约版本与变更说明；不得用功效模拟文件替代真实接入证据。

## 回件格式

请至少回传：

- 提交号与分支；
- 每项交付文件的仓库路径；
- 运行命令和完整结果；
- 证据类别（`REAL_INPUT_ADAPTER`、`BLIND_CALIBRATION` 或明确的 `WAIT_DEP`）；
- 尚未完成的里程碑和阻塞依赖。

## 当前判定

在上述回件到达并经独立 Agent 内容复核前，A-03 保持 `IN_PROGRESS`；不得填写整体 `DONE`，不得签署正式样本量、正式界值、真实校准或研究冻结。

- 发起人：Codex
- 发起日期：2026-09-21
- 独立复核：待独立 Agent 回件后登记

## 作者身份校正\n\n历史提交显示 A-03 及多数本项目交付由 Fu-junye <fujunye11@gmail.com> 完成；本机即该项目责任人的工作环境。本索取单中的作者/责任链均指傅钧烨本人对项目材料的追补与外部依赖跟进，不暗示存在未识别的第二作者。\n\n## 第二轮索取记录（2026-09-21）

已再次按责任链发起索取：

- **A-03 作者（Codex，`codex/a-03-spec`）**：请回传 A-03-REAL 真实数据计分适配器、盲态参数输入、接入质量记录；若 A-01 未就绪，请明确标注 `WAIT_DEP`，不要回填合成材料。
- **A-01 依赖门（傅钧烨本人项目责任链）**：当前注册表未登记独立领取人和分支；请由傅钧烨本人从项目历史/可访问材料补齐 L0-L3 真实/脱敏输入入口、字段字典、逐层重建报告及 A-03 可消费的版本号；没有这些材料，A-03-REAL 不可启动。
- **E-03 依赖门（傅钧烨本人项目责任链）**：当前注册表为 `BLOCKED_EXTERNAL`，未登记独立领取人和分支；请由傅钧烨本人跟进或索取 Level C 汇总门、盲态阈值冻结依据、冻结配置与签署路径；没有这些材料，A-03-CAL 不可启动。
- **U12-04 依赖确认（历史提交作者为 HuangBin）**：请确认 A-03-REAL 消费的 PANAS/SAP 契约版本和变更点；U12-04 的合成 SAP 只能作为输入约定，不能替代真实接入或校准。

截至本轮索取，仓库仍未出现 A-03-REAL/A-03-CAL 对应交付文件；A-03 继续保持 `IN_PROGRESS`。
