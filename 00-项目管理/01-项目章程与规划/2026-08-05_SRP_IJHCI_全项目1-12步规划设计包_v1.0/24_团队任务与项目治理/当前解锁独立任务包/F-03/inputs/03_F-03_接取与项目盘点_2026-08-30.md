# F-03 接取与项目盘点

> 日期：2026-08-30
> 分支：`codex/f-03-unity-baseline`
> 基线：`origin/main@26477005a7b57d4c7781a1f9711dc600b7c393ea`
> 结论：F-03 已从 `READY` 转为 `IN_PROGRESS`，当前可开始可复现 Unity 工程、测试与开发构建基线实施。

## 1. 领取记录

| 字段 | 值 |
|---|---|
| 任务 | F-03【Unity】可复现工程与测试构建基线 |
| 领取人 | Codex Agent（F-03独立对话） |
| 分支 | `codex/f-03-unity-baseline` |
| 第二复核人 | 傅钧烨（团队总监，独立第二人复核人） |
| 工作量 | 3人日 |
| 前置依赖 | 无 |
| 状态变化 | `READY -> IN_PROGRESS` |

当前主工作区位于 `codex/v-04-animatic`，并含未跟踪流程图。F-03 从最新 `origin/main` 建立独立工作树 `D:/Agent/03-SRP-f03-worktree`，不带入也不修改该工作区内容。

## 2. F-03 职责边界

F-03 负责：

- 锁定 Unity 编辑器、项目版本、直接依赖和包锁哈希；
- 建立可重复执行的 Edit Mode、Play Mode 测试入口；
- 建立 Windows 开发构建链和持续可见的 `DEV-REPLAY` 标记；
- 清除 TouchDesigner 与 Spout 运行依赖，并提供静态扫描证据；
- 保持正式模式在缺少 manifest、正式运行组件或资产门未关闭时失败关闭；
- 输出环境报告、测试 XML、构建日志、截图、扫描报告和正式门负测试报告。

F-03 不负责：

- 选择天气方案、设计场景机制或修改四层映射；
- 实现 UDP 5006、TCP 5010、ACK、回执和重连；
- 实现 `SceneAdapter` 四层运行接口；
- 推进研究顺序、计时、随机化或交互状态估计；
- 放行来源未确认的资产，或把开发回放升级为正式运行证据。

## 3. 项目任务状态

领取后注册表共 57 项：

| 状态 | 数量 | 当前关键任务 |
|---|---:|---|
| `DONE` | 9 | F-01、F-02、P-01、P-02、R-01、V-01、V-02、V-03、W-01 |
| `IN_PROGRESS` | 2 | F-03、V-04 |
| `IN_REVIEW` | 3 | F-04、G-01、G-02 |
| `READY` | 1 | F-05 |
| `WAIT_DEP` | 25 | 包含 U-01、U-02、V-05 及后续装配任务 |
| `WAIT_DEP_EXTERNAL` | 4 | 等待外部条件 |
| `BLOCKED_EXTERNAL` | 13 | 外部硬门未关闭 |

F-03 完成后只关闭以下依赖中的 F-03 一项：

- U-01 仍需 F-01、F-05、P-01；
- U-02 仍需 F-05 与 V-03；
- V-05 仍需 U-02 与 V-04。

因此 F-03 完成不会自动解锁完整 Unity 运行链或正式构建。

## 4. Unity 工程现状

### 4.1 环境与包锁

| 项目 | 当前值 |
|---|---|
| 项目版本 | Unity `6000.4.9f1`，revision `f7258d6eebbe` |
| 本机编辑器 | `D:/UnityEngine/6000.4.9f1/Editor/Unity.exe`，产品版本与项目一致 |
| 直接依赖 | 57 项 |
| `ProjectVersion.txt` SHA-256 | `ccb1d5360e2f76b5696673c1e0b8c101d21d855c074ee892e325c25c865d2775` |
| `manifest.json` SHA-256 | `9deae9423f2e7ba3119ae58cfcd381f8025363a22eb54439e082871e5c8fdb6e` |
| `packages-lock.json` SHA-256 | `b08ec61e340d00f0d7624f8fdb7c78bb9354cfd4b0abb385189318f1227f62f4` |

编辑器版本与本机安装一致，但尚无独立环境报告、恢复脚本和包锁漂移负测试。

### 4.2 工程资产与代码

Git 跟踪的 Unity 工程文件共 298 个，其中：

- 14 个场景文件；
- 40 个 C# 文件，其中 29 个为历史 Editor 工具；
- 41 个 PNG 文件；
- 5 个 DLL 文件；
- 0 个测试 C# 文件；
- 0 个程序集定义文件。

四个天气场景文件均存在，但 `EditorBuildSettings.asset` 当前只启用 `SampleScene.unity`。Storm 场景仍含历史 `WeatherController`；四个天气场景均未出现 `FormalRuntimeController`。因此现有场景不能作为正式运行候选。

### 4.3 测试与构建

- Unity Test Framework `1.6.0` 已锁定，但仓库没有 Edit Mode 或 Play Mode 测试脚本和测试程序集；
- 当前未发现基于 `BuildPipeline.BuildPlayer` 的确定性命令行开发构建入口；
- `playModeTestRunnerEnabled` 当前为 0；
- 当前未发现 `DEV-REPLAY` 构建标记实现；
- `FormalBuildGate.cs` 已存在，并要求四场景启用、历史运行组件不存在、正式运行组件存在且资产门通过；按当前工程状态应失败关闭。

### 4.4 TouchDesigner、Spout 与历史运行链

当前仍存在：

- `jp.keijiro.klak.spout` 直接依赖及包锁记录；
- `Assets/Scripts/SpoutReceiver.cs`；
- 历史 `WeatherController`、`UDPReceiver` 和 TouchDesigner/Spout 架构说明；
- 以 `SampleScene` 为唯一启用场景的旧构建设置。

这与 F-03 的无 TouchDesigner、无 Spout 开发构建基线不符，是本任务必须关闭的首要技术缺口。

### 4.5 资产门

资产许可台账当前为：

| 状态 | 数量 |
|---|---:|
| `CLEARED` | 3 |
| `PROJECT_ORIGINAL` | 1 |
| `REPLACE` | 3 |

三个 `REPLACE` 组覆盖本地 PNG、来源待确认的工程资产和 Roslyn 二进制。F-03 应保留这一失败关闭边界并建立可重复负测试，不负责代替资产负责人完成来源确认。

## 5. 质量缺口与实施顺序

| 优先级 | 缺口 | F-03 处理目标 |
|---|---|---|
| P0 | Spout 仍是直接包依赖，且有运行脚本 | 移除包和运行依赖，更新包锁、历史组件检查与静态扫描 |
| P0 | 没有 Unity 测试程序集和测试用例 | 建立最小 Edit/Play 测试架，输出 XML |
| P0 | 没有确定性 Windows 开发构建入口 | 建立命令行构建器、固定输出目录、日志和构建清单 |
| P0 | 没有持续可见的 `DEV-REPLAY` 标记 | 在开发启动场景和截图中固定显示，不进入正式模式 |
| P1 | 版本与包锁只有文件，没有恢复和漂移门 | 生成环境报告、哈希清单和失败关闭校验 |
| P1 | 正式门只有实现，没有专用负测试证据 | 覆盖缺 manifest、缺运行组件、资产门未关闭等失败路径 |
| P1 | 历史 Editor 工具较多，构建入口不唯一 | 新增唯一 F-03 构建入口，不清理无关历史工具 |

推荐实施顺序：环境锁与失败测试 → 无 Spout 编译基线 → Edit/Play 测试架 → `DEV-REPLAY` 开发壳 → Windows 构建链 → 正式门负测试 → 证据和第二人复核。

## 6. 当前证据等级与下一硬门

本次只完成任务接取、静态盘点和治理状态迁移。尚未运行 Unity 编译、Edit/Play 测试或 Windows Player 构建，因此当前证据等级为 `REPOSITORY_STATIC_INVENTORY`。

下一硬门：在独立工作树内先建立失败测试与环境锁报告，随后完成无 TouchDesigner、无 Spout 的 Unity 编译和最小开发构建；构建成功后仍需补齐测试 XML、日志、截图、正式门负测试、固定提交、推送和傅钧烨第二人复核，才能转入 `IN_REVIEW`。
