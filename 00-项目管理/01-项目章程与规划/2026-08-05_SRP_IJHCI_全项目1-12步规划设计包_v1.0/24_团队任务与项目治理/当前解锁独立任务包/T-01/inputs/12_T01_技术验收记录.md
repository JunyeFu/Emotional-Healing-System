# T-01 技术验收记录

> 状态：实现候选已完成，等待独立Agent复核与真实第二人签收。

## 接口与权限

- 公共调用保持`validate_message("telemetry_frame", payload)`不变；F-01/F-05合同未修改。
- 正式模式只接收v2.2；`dev_replay`可接收v2.1，但实际cycle/step明确显示`UNAVAILABLE`且不推断。
- UDP In DAT仅绑定`127.0.0.1:5005`；每个数据报进入统计，显示刷新最多20Hz。
- `telemetry`仅含合同过滤后的原帧；丢包、重复、乱序、重连、帧龄和界面状态仅存在于`display_only`。
- 节点清单没有网络输出、Spout、文件输出或T-02请求回调，TD不能写回Python会话权威。

## 实机结果

- TouchDesigner：2025.32820，最终关闭后从`.toe`重开；22节点、零节点错误、零脚本错误，重开报告`PASS`。
- 正常链路：真实`SessionCore + TelemetryPublisher`向UDP 5005持续发送v2.2，面板为`LIVE`。
- F-05五帧：storm双`hold`、fade双`inhale`及空实际身份由专项测试和循环回放覆盖。
- 异常链路：固定证据显示`lost=2`、`duplicate=1`、`out_of_order=1`；停止超过2秒进入`DISCONNECTED`，新clock domain合法帧恢复`LIVE`并累计重连。
- 外部录像：TD附带ffmpeg禁用`gdigrab`，因此使用实机回放固定截图在运行制品外编码11.5秒MP4；未向TD工程加入文件输出节点。

## 回归结果

| 门 | 结果 |
|---|---|
| T-01专项 | `17 passed` |
| F-04专项 | `21 passed` |
| F-05合同 | `87 passed` |
| Session Core | `115 passed` |
| Session Store | `70 passed` |
| `verify_f05_v22.py` | `PASS`；22 fixture；Unity/TD各5帧 |
| 全项目 | `470 passed` |
| F-04签署树 | 相对`43a63a1`零差异 |

## 制品身份

- `T01_TelemetryPanel.toe` SHA-256：`38ECA7FAD11A989DA4EF3923A4277E557A966586CDD2F7C2F1EC251CCFE57E44`
- `T01_TelemetryPanel.tox` SHA-256：`F0614B20CF71E3C6E83B98D4CCC8A3CAD2F953A557DC2AAAEF809B15530AB8AE`
- `fixture_replay.mp4` SHA-256：`580DB8EEB06E957D3B277D3D7CFBBCAEA6261EBCAAB6E0CEC59094EDCEB1DD5A`

若实现、合同适配、fixture、`.toe`或`.tox`发生实质变化，本记录对应的独立复核与第二人签收必须失效并重做。

## 边界

本候选不声明真实Polar/呼吸设备链、Unity联合运行、T-02请求通道、设备到屏幕外部延迟、正式构建、科学有效性或`LIVE_E2E`完成。
