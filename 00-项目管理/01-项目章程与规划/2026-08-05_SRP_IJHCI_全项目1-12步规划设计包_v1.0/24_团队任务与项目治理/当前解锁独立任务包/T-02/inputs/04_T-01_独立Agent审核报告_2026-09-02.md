# T-01 独立Agent审核报告

> 审核对象：`codex/t-01-telemetry-panel@8790cd3ae4db3543c038efc21deec635605cb06f`
> 审核角色：Codex Agent（T-01独立复核）
> 结论：`PASS`
> 本报告不构成傅钧烨第二人签收。

## 候选演进

第一候选`9d6c9e0b1d303e454c7ac319d0df053aa71046e9`独立复核为`FAIL/P1`：洁净Windows checkout中4项host JSON和1项ffconcat发生LF到CRLF转换，导致证据清单SHA/bytes不一致。运行、测试和TD实机门虽通过，但该候选不得签收。

修复通过目录级`.gitattributes`精确固定上述文本证据的原始字节；没有修改适配器、fixture、`.toe/.tox`或公共合同。新候选`8790cd3`必须并已接受完整重审。

## 第二轮独立结果

| 门 | 独立结果 |
|---|---|
| P0/P1/P2/P3 | 均无候选缺陷 |
| 审计环境 | 全新`D:\Agent\03-SRP-t01-audit-v2` detached工作树；全程只读；最终洁净 |
| 证据清单 | 23项原始bytes/SHA-256，`BAD=0` |
| T-01专项 | `17 passed in 0.11s` |
| F-04专项 | `21 passed in 0.11s` |
| F-05合同 | `87 passed in 1.84s` |
| Session Core | `115 passed in 0.67s` |
| Session Store | `70 passed in 4.78s` |
| F-05验证 | `PASS`；v2.1未漂移；schema可重建；22 fixture；Unity/TD各5帧 |
| 全项目 | `470 passed in 17.60s` |
| F-04签署树 | 相对`43a63a1`零差异 |
| `git diff --check` | `PASS` |

## 接口与权限

- 公共入口保持`validate_message("telemetry_frame", payload)`。
- 正式模式拒绝v2.1；`dev_replay`兼容v2.1但step显示`UNAVAILABLE`且不推断。
- 快照递归不可变，合同过滤后的`telemetry`与本地`display_only`分离。
- 每数据报统计、epoch、跳号、重复、乱序、2秒断流和50ms显示刷新门均被检查。
- 无UDP/TCP输出、Spout、文件输出、T-02回调或Python权威写回能力。

## TouchDesigner实机复现

- TouchDesigner 2025.32820从关闭状态打开候选TOE，并独立加载候选TOX；两者均22节点、`errors=''`、`scriptErrors=''`、1280×720。
- UDP In为`active=True`，仅绑定`127.0.0.1:5005`；`authority=PYTHON_SESSION_CORE`，`network_direction=INPUT_ONLY`。
- 真实`SessionCore + TelemetryPublisher`进入`LIVE`并显示v2.2帧。
- 异常回放实际累计`lost=2 / duplicate=3 / out_of_order=2`。
- 停发超过2秒进入`DISCONNECTED`；新clock domain恢复`LIVE`、`reconnect_count=1`且异常统计保留。
- 收尾时TD和发送器已关闭，UDP 5005释放，审计树洁净。

## 签收边界

独立复核`PASS`后仍需傅钧烨对精确候选`8790cd3ae4db3543c038efc21deec635605cb06f`完成真实第二人签收。签收前T-01保持`IN_REVIEW`，T-02保持`WAIT_DEP`。若实现、合同适配、fixture或`.toe/.tox`发生实质变化，本复核立即失效。

本报告仅证明本机TouchDesigner对Python权威v2.2遥测的只读消费、显示和异常恢复基线，不覆盖真实Polar/呼吸设备、Unity联合运行、T-02请求通道、正式构建、科学有效性或`LIVE_E2E`。
