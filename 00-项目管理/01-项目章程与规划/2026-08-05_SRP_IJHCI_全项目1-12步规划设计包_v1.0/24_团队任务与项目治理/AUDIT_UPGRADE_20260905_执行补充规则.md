# 2026-09-05 审计升级执行补充规则

> 状态：`APPROVED_FOR_BATCHED_IMPLEMENTATION`
>
> 适用范围：审计升级UP-01至UP-12及A-06，不覆盖历史签收和现行研究参数。

## 执行边界

1. 外部审计包是输入证据，不是项目权威。每项建议必须在当前工作树复现或分类后再实施。
2. 保留16项已签收任务及其精确范围；修正文案和元数据不得改写原签署内容。
3. UP-01至UP-12作为原任务的升级子交付管理，不默认增加12个顶层任务。新增顶层任务仅为A-06。
4. 阶段一是主论文可独立收尾路线；阶段二和阶段三是条件式扩展。未开展任务保留原状态，不以DONE表示跳过。
5. 主要终点、非劣界、样本量和800秒推荐流程不在本轮直接修改；统计可行性结果进入后续真人冻结门。
6. 项目用语继续遵守根目录`AGENTS.md`，审计建议N05不采纳。

## 证据规则

- 任务状态、证据状态、外部资格和研究路线分别记录，不能由单一状态字符串互相推断。
- DONE必须绑定候选身份、签收范围、复核人、复核方式和证据指纹；占位文字不能通过验证。
- 原始证据、二进制和正式制品记录字节哈希；项目文本快照可继续使用规范化文本哈希，但必须标明策略。
- READY、IN_PROGRESS和IN_REVIEW的独立包均保存`input_snapshot_id`。进行中或复核中输入变化时生成影响记录并停止刷新，不自动替换任务执行者的输入或判断。
- 机构、量表、资产用途、正式机器和工位能力按活动资格分别签署；Agent只能验证结构，不能代签外部资格。

## 批次出口

- 批次0：输入来源、24项处置和当前入口一致。
- 批次1：59项依赖图、A-06路线、任务生命周期和活动资格接口通过负测。
- 批次2：原始证据束、统计可行性、设备兼容与资源模型形成候选。
- 批次3：双条件最小闭环及真实运行证据形成候选。
- 批次4：按实际实施范围关闭A-06并构建投稿证据链。

自动检查通过只表示工程结构一致，不解除外部准入、真实设备、研究执行或论文结论门。
