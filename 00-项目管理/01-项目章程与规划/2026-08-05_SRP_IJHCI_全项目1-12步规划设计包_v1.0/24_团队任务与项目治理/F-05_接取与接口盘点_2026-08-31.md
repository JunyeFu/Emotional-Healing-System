# F-05 接取与接口盘点

> 日期：2026-08-31 +08:00
> 状态：`IN_PROGRESS`
> 领取人：Codex Agent（F-05独立对话）
> 第二复核人：傅钧烨（团队总监，独立第二人复核人）

## 1. Git与任务身份

| 项目 | 值 |
|---|---|
| 工作树 | `D:\Agent\03-SRP-f05-worktree` |
| 分支 | `codex/f-05-phase-v22` |
| 起点 | `c358a3537d3c110eb03ddcf8f4748b9b7dfb61a8` |
| 起点含义 | F-03第二人签收后的治理提交，已包含`origin/main` |
| 预计工作量 | 4人日 |
| 前置依赖 | F-01、P-01、P-02均为`DONE` |

当前V-04工作树保持隔离。V-04提交`574b8e36ff349fc83e6fdbc122baf1bb81d6a823`只作为最新状态与设计输入核对，不并入F-05领取基线；其H3合并候选等待团队总监确认。

## 2. 领取后项目状态

| 状态 | 数量 | 当前任务 |
|---|---:|---|
| `DONE` | 10 | F-01、F-02、F-03、P-01、P-02、R-01、V-01、V-02、V-03、W-01 |
| `IN_PROGRESS` | 2 | F-05、V-04 |
| `IN_REVIEW` | 3 | F-04、G-01、G-02 |
| `READY` | 0 | 无 |
| `WAIT_DEP` | 25 | 依注册表逐项等待 |
| `WAIT_DEP_EXTERNAL` | 4 | 依赖设备或外部运行条件 |
| `BLOCKED_EXTERNAL` | 13 | 外部门未闭合 |

F-05领取不会自动解锁下游。只有F-05实现、证据、独立复核和傅钧烨第二人签收全部通过后，U-01、U-02、T-01和U-07才关闭各自的F-05依赖；它们仍可能受其他依赖阻断。

## 3. 当前接口事实

| 层 | 已有事实 | F-05缺口 |
|---|---|---|
| F-01 v2.1 | Schema与Python参考实现已签收；遥测只有粗相位与步骤内未定义的进度 | 不能区分`storm`两个停留或`fade`两次吸气 |
| P-01 | `validate_message()`是统一校验入口；核心与传输全局固定`SCHEMA_VERSION=2.1` | 需要按manifest保存会话版本，并让控制、握手和遥测使用同一版本 |
| P-02 | 所有manifest和遥测先经P-01合同seam再保存；重放保持提交输出 | 当前会拒绝v2.2，尚无新增字段的确定性重放证据 |
| 呼吸配置 | `protocol_config_v1.1.json`只含模块段时长和传输参数 | 缺少独立步骤表、步骤时长、版本与哈希；`fade`精确时长仍开放 |
| Unity | F-03只提供`DEV-REPLAY`技术底座；U-01/U-02尚未实现 | F-05只交付合同fixture和失败样例，不写Unity运行逻辑 |
| TouchDesigner | F-04是只读静态工作台；T-01尚未实现 | F-05只交付合同fixture和失败样例，不写UDP 5005消费者 |
| V-04 | H3候选已形成24节点交接，仍待团队总监确认 | 不作为F-05已签收运行参数；步骤身份沿用已签收V-02/V-03输入 |

## 4. 接口对齐结论

合同seam冻结为`validate_message(message_type, payload)`，版本分派位于seam内部；P-01与P-02公共方法不扩张。v2.2新增manifest呼吸配置版本/哈希和遥测目标/实际周期及步骤字段，字段语义、步骤表、暂停重连重放规则与消费者责任见[接口对齐基线](../../../../02-技术研发/05-通信协议/contracts/F-05_v2.2接口对齐基线.md)。

当前唯一需要用户或协议责任人进一步冻结的内容是`fade`三个步骤的精确秒数。F-05不得根据常见双吸节奏自行补造该值。

## 5. 实施队列

1. 为v2.2建立失败测试、Schema和正负fixture，同时锁定v2.1文件哈希不变。
2. 建立独立呼吸步骤配置Schema；等待并写入`fade`精确时长后冻结配置哈希。
3. 将版本分派收敛到合同seam，迁移P-01会话版本和传输握手。
4. 迁移P-02保存与重放，验证暂停、恢复、重连首帧、重复及乱序输入。
5. 生成Unity/TD消费fixture、迁移矩阵、专项验证报告和哈希证据。
6. 固定候选提交并交独立Agent与傅钧烨复核；复核前保持`IN_PROGRESS`。

## 6. 证据边界

本文件证明任务已领取、现状已盘点且接口责任已对齐；尚未证明v2.2 Schema、运行适配、确定性重放、非Python消费或正式模式已经通过。F-05不会修改公共v2.1合同，不实现U-01、U-02、T-01或U-07的运行职责。
