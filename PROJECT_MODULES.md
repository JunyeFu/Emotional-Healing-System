# SRP 项目模块地图

> 本文件是当前项目结构、模块职责和协作接口的权威入口。历史规划与阶段背景见 `PROJECT_FRAMEWORK.md`，实时任务状态见 `00-项目管理/看板与进度/当前阶段看板.md`。

## 1. 系统闭环

```text
设备或 Mock
  -> M02 数据采集
  -> M03 交互状态估计
  -> M04 通信与记录
       -> UDP 5005 -> M05 TouchDesigner 引导与监控 -> Spout
       -> UDP 5006 -> M06 Unity 天气视觉 <- Spout
       -> CSV       -> M08 验证与证据

M01 体验设计定义四种天气及其核心视觉机制。
M00 项目管理维护范围、进度、风险和质量门禁。
M07 开发桥接只辅助构建，不属于演示运行链路。
M09 成果交付汇总经验证的系统、文档和媒体材料。
M10 文献证据为设计与交互状态估计提供可追溯依据。
```

## 2. 模块总览

| ID | 模块 | 目录 | 对外接口 | 当前验证入口 |
|---|---|---|---|---|
| M00 | 项目管理 | `00-项目管理/` | 范围、看板、风险、质量门禁、交接记录 | `00-项目管理/README.md` |
| M01 | 需求与体验设计 | `01-需求与设计/` | 四种天气、交互流程、视觉机制与文献支撑 | `01-需求与设计/README.md` |
| M02 | 数据采集 | `02-技术研发/01-数据采集/` | 10 Hz `RawFrame` 与数据源状态 | 采集模块 pytest |
| M03 | 交互状态估计 | `02-技术研发/02-信号处理/` | `ProcessedFrame`、四维评分、`calm_index` | 估计模块 pytest |
| M04 | 通信与记录 | `02-技术研发/05-通信协议/` | UDP JSON v1.2、CSV 固定列 | 通信模块 pytest；字段冻结表 |
| M05 | TouchDesigner 引导与监控 | `02-技术研发/03-TouchDesigner/` | UDP `5005` 输入、`SRP_BreathGuide_Main` Spout 输出 | TD 工程人工复核 |
| M06 | Unity 天气视觉 | `02-技术研发/04-Unity视觉/` | UDP `5006` 与 Spout 输入、四天气画面输出 | Unity Play Mode 复核 |
| M07 | 开发桥接 | `02-技术研发/06-MCP开发桥接/` | TD/Unity 构建与检查工具说明 | 各桥接手册中的连通性检查 |
| M08 | 验证与证据 | `03-测试与实验/`、各 `tests/` | 测试结果、运行记录、数据字典与证据包 | `03-测试与实验/README.md` |
| M09 | 成果交付 | `04-成果与交付/` | 可演示系统、报告、媒体和交付清单 | `04-成果与交付/README.md` |
| M10 | 文献证据 | `srp参考文献/` | 来源、摘要、引用映射与评分依据 | `srp参考文献/README.md` |

## 3. 运行模块接口

### M02 数据采集

- 输入：呼吸、心电、EDA 的设备适配器，或四天气 Mock 数据。
- 输出：统一时钟下的 `RawFrame` 和数据源状态。
- 内部实现：设备驱动、设备管理、环形缓冲、帧时钟、Mock 生成。
- 约束：设备不可用时允许降级，不改变下游字段契约。

### M03 交互状态估计

- 输入：M02 的标准帧。
- 输出：四维评分、`calm_index`、天气强度和引导参数。
- 内部实现：信号预处理、特征提取、评分配置和维度说明。
- 约束：输出必须有界；消费级设备数据仅用于交互状态估计。

### M04 通信与记录

- 输入：M03 的评分帧及数据源元信息。
- 输出：发往 TD/Unity 的同构 JSON，以及可复核 CSV。
- 权威契约：`02-技术研发/05-通信协议/UDP字段冻结_v1.2.md`。
- 约束：字段先冻结后实现；接收端忽略未知新增字段。

### M05 TouchDesigner 引导与监控

- 输入：UDP `5005` 的 JSON。
- 输出：实验员监控信息和 Spout 呼吸引导纹理。
- 约束：不承担天气画面渲染；Spout Sender 固定为 `SRP_BreathGuide_Main`。

### M06 Unity 天气视觉

- 输入：UDP `5006` 的 JSON 与 TD Spout 纹理。
- 输出：用户看到的四天气场景、旅人动画和环境提示。
- 约束：每种天气仅保留一个核心视觉机制；提示从环境自然出现。

## 4. 模块依赖规则

1. `M02 -> M03 -> M04` 是单向 Python 主链路，入口为 `02-技术研发/main.py`。
2. M05 与 M06 只依赖 M04 的冻结字段，不直接读取 M02/M03 内部对象。
3. M06 可接收 M05 的 Spout 纹理，但天气状态仍以 M04 的 UDP 字段为准。
4. M07 不得成为演示运行的必需依赖；桥接失效时，已构建工程仍应可运行。
5. M08 通过模块接口验证可观察结果，不依赖 Unity 缓存或 TD 临时生成物。
6. M09 只接收已通过相应门禁的材料，不把计划状态写成完成状态。

## 5. 文件归属规则

| 内容 | 应放位置 |
|---|---|
| 范围、计划、风险、交接 | `00-项目管理/` 对应子目录 |
| 天气、交互、视觉设计 | `01-需求与设计/` |
| Python、TD、Unity、协议与开发桥接 | `02-技术研发/` 对应运行模块 |
| 测试方案、数据字典、测试结果、运行证据 | `03-测试与实验/` |
| 报告、演示、截图、视频、交付清单 | `04-成果与交付/` |
| 文献原文、摘要、引用映射 | `srp参考文献/` |
| Codex 当前任务事实与验证记录 | `work/` |

根目录只保留项目入口、运行入口、约束文件和跨模块总览。Unity `Library/`、`Temp/`、`Logs/`、`obj/` 等生成目录不是项目模块，也不作为交付内容。

## 6. 最小验证

```powershell
Set-Location 'D:\Agent\03-SRP'
py -3.14 -m pytest '02-技术研发/01-数据采集/tests' '02-技术研发/02-信号处理/tests' '02-技术研发/05-通信协议/tests' '02-技术研发/tests' -q
git diff --check
git status --short
```

TD、Spout 和 Unity 的同机闭环按 `README_run.md` 人工复核；Python 测试通过不能替代画面与纹理链路验证。
