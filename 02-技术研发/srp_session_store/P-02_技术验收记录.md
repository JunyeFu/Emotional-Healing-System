# P-02 Manifest、L0/L1追加记录与确定性重放技术验收记录

> 状态：`TECHNICAL_IMPLEMENTATION_CANDIDATE`；治理状态在提交B迁移为`IN_REVIEW`后生效。

## 验收对象

- 分支：`codex/p-02-session-store`
- 基线：P-01治理提交`3163520a4d485c859267b4b87c6cb370138e8dce`
- 实现目录：`02-技术研发/srp_session_store/`
- 测试目录：`02-技术研发/tests/session_store/`
- 实现提交：待提交A后回填

## 边界

本候选实现不可覆盖manifest、L0/L1追加、检查点、封存、数据恢复和确定性重放。P-01仍是流程、时钟和控制权威；旧CSV入口未改写。

本记录不表示设备链、Unity画面、TD操作台、X-01随机化适配器、正式跨进程链路或`LIVE_E2E`已经完成。正式运行仍受G-02、X-01、Unity客户端和外部条件约束。

## 验收矩阵

| 要求 | 结果 | 证据 |
|---|---|---|
| AC1 中断恢复不覆盖旧数据 | PASS | 部分尾行和正常中断均保留旧段原始字节，新增段记录`PROCESS_INTERRUPTED`并封存 |
| AC2 校验和发现改写 | PASS | 记录改写、检查点改写、乱序、缺失和封存哈希漂移均由完整性校验拒绝 |
| AC3 相同输入重放相同状态事件 | PASS | P-01 golden轨迹46次核心调用重放输出哈希一致 |
| manifest正式门 | PASS | 开发适配器不可声明正式能力；正式工厂要求仓库外路径、账号、加密、ACL和G-02角色能力 |
| L0/L1字段治理 | PASS | 固定逻辑来源、来源策略、每来源序号、缺失原因码、G-02禁入字段和P-02附加禁入字段均失败关闭；错误路径不回显动态键 |
| 800秒等效压力 | PASS | 32万PLUX样本、10.4万Polar样本、1.6万L1帧，计数和完整性通过；分段内存采样用于识别持续增长趋势 |

## 候选证据

- `fixtures/golden/session-archive-v1/evidence.json`
- `evidence/synthetic_stress_report.json`
- `contracts/archive-envelope-v1.schema.json`
- `contracts/append-record-v1.schema.json`
- `contracts/session-seal-v1.schema.json`

## 验证结果

- P-02定向测试：`42 passed`。
- P-02、P-01、F-01与G-02跨模块回归：`288 passed`。
- 全仓库Python回归：`331 passed`。
- 800秒合成压力：PLUX `320000`样本、Polar `104000`样本、L1 `16000`帧，`integrity_valid=true`。
- 独立Agent经过四轮只读复查，初次发现的4个P1和2个P2全部关闭，最终结论：`PASS_FOR_MODEL_INDEPENDENT_REVIEW`。
- `compileall`、协议权威校验、`git diff --check`及本任务不合规措辞扫描通过。

实现提交SHA将在提交B回填。自动测试只形成Python存储技术候选证据，第二人签收前P-02不得转为`DONE`。
