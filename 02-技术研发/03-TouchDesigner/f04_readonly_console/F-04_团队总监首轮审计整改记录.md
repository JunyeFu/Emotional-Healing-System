# F-04 团队总监首轮审计整改记录

> 审计角色：独立 Agent，作为团队总监检验
>
> 首轮审计任务：`审计 F-04 TouchDesigner工作台`
>
> 首轮任务 ID：`01a04b7e-3ecf-7470-994c-6ca37c9ecd5b`

## 首轮结论

首轮结论为 `FAIL`。AC3、AC5、AC6 通过；AC1、AC2、AC4、AC7 未通过。
该结论替代升级前候选的验收判断，并触发本轮整改。独立 Agent 未修改工程。

## 缺口与整改

| 首轮缺口 | 本轮整改 | 当前机器证据 |
|---|---|---|
| 页面与场景导航重叠，`ConsoleShell` 未形成最终输出 | 10 个页面按钮按 2×5 排列，5 个场景按钮单排；按钮使用稳定节点名；`shell_viewer` 只查看 `ConsoleShell`，`display_out` 仅连接该视图 | 重开报告中页面/场景导航可见且坐标唯一，`shell_is_final_output=true` |
| 页面主要是文本转储或通用矩形 | 10 页×5 场景建立页面专属图形角色；呼吸页保留真实 CHOP 双通道曲线；其他页使用状态卡、条形、轨迹或控件卡片 | `page_graphics_complete_50=true`，14 张截图哈希全部唯一 |
| 人工标记与中止缺少可见禁用控件 | 页面显示两组禁用控件卡片；对应 `buttonCOMP` 固定为 `enabled=false`，完整标签为 `T-02 NOT ACTIVE`；F-04 未定义请求发送回调 | 权限清单与重开报告 `manual_controls_visible_disabled_no_callbacks=true` |
| 独立任务包把未复核项自动勾选 | 渲染器支持逐项结果；F-04 的 AC7 与第二人证据保持未勾选，直到独立复核形成结论 | 新独立任务包的 AC7 标记为 `PENDING_DIRECTOR_REVIEW` |
| README 含机器绝对路径，页面有裁切 | README 改为仓库根目录占位；页面文字压缩并将图形区独立到右侧；人工控件标签使用短状态，完整职责状态保留在左侧 | 10 张 GOOD 页面截图与 4 张差异截图 |

## 整改后机器结论

- F-04 专项：21/21 通过。
- F-01 合同回归：49/49 通过。
- P-01/P-02 相关回归：166/166 通过。
- TouchDesigner 2025.32820 构建、保存、关闭、重开后，50/50 页面与场景组合通过，节点错误为 0。
- TD 关闭后，5005 与 9981 均无 TCP/UDP 监听。

因此 AC1 至 AC6 当前为机器候选 `PASS`。AC7 仍为
`PENDING_DIRECTOR_REVIEW`，必须由独立 Agent 基于本轮候选重新打开 `.toe`、
切换全部页面与场景、检查权限后给出 PASS/FAIL；本记录不预先代替该结论。

## 证据边界

本轮只证明静态 fixture、合同兼容、只读权限和本机 TD 工作台可复现，不证明
真实设备、正式 20 Hz 消费、请求处理、Unity 联合运行、LIVE_E2E、科学有效性
或发布完成。
