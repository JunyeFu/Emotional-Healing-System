# F-01运行合同v2.1技术验收记录

> 日期：2026-08-07
> 技术状态：`MODEL_INDEPENDENT_REVIEW_PASS_PENDING_HUMAN_SECOND_REVIEW`
> 任务注册状态：`READY`（保留到第二人复核完成）
> 分支：`codex/f-01-runtime-contract`

## 交付范围

- `runtime_contract.py`：无第三方运行依赖的严格校验、兼容字段过滤和控制事件幂等账本。
- `contracts/runtime-contract-v2.1.schema.json`：Draft 2020-12组合Schema。
- `contracts/fixtures/`：7个合法/兼容fixture和4个定向失败fixture。
- `contracts/fixture_hashes.sha256`：11个fixture的SHA-256证据。
- `MIGRATION_v1.2_to_v2.1.md`：旧开发链到正式合同的分阶段迁移与回滚说明。
- `contracts/port-registration-v2.1.md`：UDP 5005/5006和TCP 5010登记证据。

## AC结果

| AC | 技术结果 | 证据 |
|---|---|---|
| AC1 合法fixture全通过 | PASS | `tests/contract/test_runtime_contract.py`覆盖manifest、控制、ACK、遥测、策略决策和渲染回执 |
| AC2 缺字段、错版本、正式Mock和重复控制失败关闭 | PASS | 4个非法fixture、重复事件与陈旧序号负测试 |
| AC3 未知兼容字段安全忽略 | PASS | `telemetry-forward-compatible.json`经校验后删除`future_display_hint` |

## 验证结果

```text
py -3.14 -m pytest 02-技术研发/05-通信协议/tests/contract -q
42 passed

py -3.14 -m pytest 02-技术研发/01-数据采集/tests 02-技术研发/02-信号处理/tests 02-技术研发/05-通信协议/tests 02-技术研发/tests -q
84 passed
```

## 未越界事项

- 没有实现P-01会话状态机、P-02追加日志、U-01 Unity网络客户端或T-01 TD面板。
- 没有把v1.2 Mock原型升级描述为真实设备或完整运行证据。
- TCP 5010只完成配置预留和无监听冲突预检，没有声称服务已运行。
- F-01在第二人检查接口、fixture和失败语义前不得转为`DONE`，P-01、P-02、G-02、U-01和T-01继续保持依赖等待。

## 2026-08-07独立Agent复查与修复

独立Agent针对提交`cef7d54ef984a8bd141cb2c9b9d379c4f6e6f299`给出`FAIL_BLOCK_GOVERNANCE_CLOSURE`，发现3项P1和2项P2。该结论不作为真实团队第二人签署。

| 独立发现 | 修复 |
|---|---|
| Schema与Python接受/拒绝边界漂移 | Schema冻结无状态机器边界；Python标准库实现补齐类型、可空、跨字段和正式manifest规则；Draft 2020-12测试对全部fixture和差分负例执行双校验 |
| fixture哈希受Windows换行影响 | 哈希改为`canonical-lf-v1`并增加`.gitattributes`；已对Git blob验证11/11一致 |
| 重复或陈旧控制无审计结果 | `ControlEventLedger.audit_log`在失败关闭前写入`duplicate_ignored`或`rejected`结构化记录 |
| 畸形枚举泄漏原生异常 | 所有枚举、天气序列和候选动作先检查类型，再执行集合运算 |
| 测试未实际执行JSON Schema | 测试依赖固定`jsonschema[format]==4.25.1`，运行`Draft202012Validator.check_schema`及实际消息校验 |

修复后必须由新的独立Agent重新审查；通过也只代表模型独立复核，不替代下方真实团队第二人签署。

第二轮独立Agent再次给出`FAIL_BLOCK_GOVERNANCE_CLOSURE`，并提供整数语义、嵌套额外字段、构建字符串换行、阶段一概率、回放来源、双向降级字段和applied审计顺序反例。对应修复均已进入合同与差分回归：

- Python按JSON Schema数学整数语义接受有限整数值，包括`1.0`；账本内部规范为`int`。
- `module_durations`拒绝任何未声明嵌套字段；正式构建标识拒绝CR/LF。
- 阶段一`behavior_probability`必须严格等于`1 / len(candidate_actions)`。
- `dev_mock`、`dev_replay`和`formal_*`分别绑定`mock`、`replay`和`real`来源策略；回放禁止Mock设备源。
- `fallback_applied=false`要求`fallback_reason=null`。
- `applied`审计记录在账本状态推进前追加，并由观察型测试确认顺序。

第三轮独立Agent复查仍是技术关闭门禁。

第三轮独立Agent确认上述反例均已关闭，只发现Python对400位整数执行浮点有限性检查时泄漏`OverflowError`。修复后Python对原生`int`不再转换为浮点数，并新增任意精度整数差分测试。

第四轮独立Agent结果：

- 合同语义、前两轮反例、任意精度整数、AC1至AC3、42项合同测试、84项项目回归、PowerShell消费者及11个Git blob哈希均通过；
- 唯一阻断是最终代码修改后F-01快照尚未重新生成；
- 重新生成任务包并由独立Agent确认哈希漂移消失后，技术状态才可转为模型独立复核通过。

最终快照重建后，同一独立Agent只读复核两个任务包校验器及核心源/快照哈希，确认无P0至P2发现，并给出`PASS_FOR_MODEL_INDEPENDENT_REVIEW`。该结论关闭技术模型复核门禁，但不能替代真实团队第二人的身份、责任和签署；任务注册状态现为`IN_REVIEW`，不可重复领取。

## 2026-08-08 缺陷回归增量

- 合同新增正式阶段/模式对应、控制与ACK时间顺序、ACK结果一致性、遥测三时钟顺序、模块控制非空载荷及设备配置嵌套字段负例。
- CSV列由当前`ScoreFrame.to_dict()`字段冻结，缺字段和未知字段均以`CSV_SCHEMA_MISMATCH`失败关闭。
- 根目录`pytest.ini`隔离TD vendor测试环境，当前项目回归为`98 passed`；这仍不是Unity、TD或真实设备`LIVE_E2E`。
- F-01继续等待真实团队第二人复核；本增量不得把`IN_REVIEW`自动转为`DONE`。

## 第二人复核清单

- [ ] 使用非实现者身份逐项复核Schema与Python校验器字段一致性。
- [ ] 用至少一种非Python消费者读取合法和前向兼容fixture。
- [ ] 确认重复控制不会推进状态，且能形成`duplicate_ignored`或等价审计回执。
- [ ] 确认正式manifest无法携带Mock或未知构建。
- [ ] 签署复核人、命令、结果和发现后，才允许任务注册表转为`DONE`。
